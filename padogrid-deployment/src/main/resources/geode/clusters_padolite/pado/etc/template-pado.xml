<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pado SYSTEM "../pado1_0.dtd">

<pado id="${GRID_ID}" location="local">
	
	<app-list>
		<app id="sys">
      <grid-list id="sys grid list" default-grid-id="${GRID_ID}">
        <grid id="${GRID_ID}" routing-weight="1">
					<description>The default grid id is itself, i.e., no children.</description>
				</grid>
				<description>Allowed grids for sys</description>
			</grid-list>
		</app>
	</app-list>
	
	<path-list>
	</path-list>
	
  <gemfire root-region-path="${GRID_ID}">
	</gemfire>

</pado>
