apiVersion: v1
kind: Service
metadata:
  name: hazelcast-jet-service-lb
spec:
  type: LoadBalancer
  selector:
    app: hazelcast-jet
    role: hazelcast-jet
  ports:
  - protocol: TCP
    port: 5701
    nodePort: 30200
