kind: ConfigMap
apiVersion: v1
metadata:
  name: hazelcast-jet-enterprise-management-center-configuration
  labels:
    app: hazelcast-jet
data:
  hazelcast-client.yaml: |-
    hazelcast-client:
      cluster-name: ${CLUSERT_NAME}
      network:
        kubernetes:
          enabled: true
          namespace: ${NAMESPACE}
          resolve-not-ready-addresses: true
          service-name: ${SERVER_NAME}
